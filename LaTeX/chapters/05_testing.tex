\chapter{Collaudo e Testing}

Il testing del sistema \textbf{EscapeManager} utilizza i framework \textbf{JUnit 5} e \textbf{Mockito} ed è organizzato su più livelli. Lo scopo, oltre a trovare eventuali bug, è dimostrare che l'architettura funziona come previsto dai requisiti (Capitolo 2).

\section{Strategia di Verifica}
La strategia di test è organizzata su quattro livelli complementari:
\begin{itemize}
    \item \textbf{Unit Test (White-Box):} verificano le logiche pure del Domain Model e dei Design Pattern in completo isolamento.
    \item \textbf{Mock Test (Mockito):} testano i Controller sostituendo i DAO reali con oggetti simulati, isolando la logica di business dal database.
    \item \textbf{Integration Test (DAO/DB):} verificano il mapping ORM e la correttezza delle query principali su database reale.
    \item \textbf{Functional Test (Black-Box):} esercitano i Controller come surrogati della UI, in linea con i template UC.
\end{itemize}

\section{Setup e Dati di Test}
Per garantire test ripetibili, prima di ogni test di integrazione o funzionale il database viene riportato ad uno stato noto tramite \texttt{DatabaseHelper.resetTestDatabase()}, che esegue TRUNCATE e reinserisce dati minimi consistenti (una stanza e due clienti).

\section{Testing Strutturale (White-Box)}

I test strutturali (o Unit Test) si sono concentrati sulle classi del \texttt{Domain Model}, con l'obiettivo di massimizzare la copertura (Code Coverage) dei rami condizionali e verificare la correttezza algoritmica, in totale isolamento dalle dipendenze esterne.

Particolare attenzione è stata dedicata al collaudo dei Design Pattern. Il Listing \ref{lst:test_strategy} mostra un test parametrizzato per la verifica delle politiche di prezzo dinamiche (\textit{Strategy Pattern}).

\begin{lstlisting}[caption={Unit Test per la logica di calcolo del prezzo (Strategy Pattern)}, label={lst:test_strategy}]
class PricingStrategyTest {

    @Test
    void testTariffaBaseCalcoloCorretto() {
        // Setup
        Stanza stanza = new Stanza("R01", "FI01", "Horror Asylum", 6, 20.0);
        stanza.setPricingStrategy(new TariffaBase());
        
        // Execution: 4 giocatori a 20 euro base = 80 euro
        double prezzoCalcolato = stanza.calcolaPrezzo(4);
        
        // Assertion
        assertEquals(80.0, prezzoCalcolato);
    }
}
\end{lstlisting}

Oltre al calcolo del prezzo, i test unitari verificano anche le transizioni di stato della \texttt{Stanza} (State Pattern, con 11 test per tutte le transizioni valide e illegali), il meccanismo Observer della \texttt{WaitingList} (attach, detach, notify), e la costruzione fluente tramite \texttt{Builder Pattern}.

\section{Testing con Mock (Mockito)}

I Controller sono collaudati in isolamento tramite \textbf{Mockito}, simulando il comportamento delle interfacce DAO per verificare la logica di business indipendentemente dal livello di persistenza.

L'annotazione \texttt{@ExtendWith(MockitoExtension.class)} abilita l'integrazione automatica con JUnit 5, mentre \texttt{@Mock} inietta i DAO simulati e \texttt{verify()} asserisce che i metodi attesi siano stati invocati.

\begin{lstlisting}[caption={Mock Test: flusso base UC1 con Mockito}, label={lst:test_mock}]
@ExtendWith(MockitoExtension.class)
class PrenotazioneControllerMockTest {

    @Mock private PrenotazioneDAO prenotazioneDAO;
    @Mock private StanzaDAO stanzaDAO;
    private PrenotazioneController controller;

    @BeforeEach
    void setUp() {
        controller = new PrenotazioneController(prenotazioneDAO, stanzaDAO);
    }

    @Test
    void testPrenotazioneRiuscita_FlussoBase_UC1() {
        Stanza stanza = new Stanza("R01", "FI01", "Horror", 6, 20.0);
        when(stanzaDAO.findById("R01")).thenReturn(stanza);
        when(prenotazioneDAO.isSlotOccupato(eq("R01"), any())).thenReturn(false);

        Prenotazione result = controller.effettuaPrenotazione("C01", "R01",
                LocalDateTime.of(2026, 10, 31, 21, 0), 4);

        assertNotNull(result);
        assertEquals(80.0, result.getPrezzoTotale());
        verify(prenotazioneDAO, times(1)).save(any(Prenotazione.class));
    }
}
\end{lstlisting}

\section{Testing di Integrazione (DAO e Database)}

Per verificare il corretto interfacciamento tra l'applicazione Java e il database PostgreSQL (livello ORM), sono stati implementati test di integrazione sulle classi DAO.
Per garantire la \textbf{ripetibilità} e l'isolamento, il costrutto \texttt{@BeforeEach} invoca \texttt{DatabaseHelper.resetTestDatabase()}, che esegue TRUNCATE e reinserisce un dataset minimo prima di ogni metodo di test.

\begin{lstlisting}[caption={Integration Test per StanzaDAOPostgres con reset dello stato}, label={lst:test_dao}]
class StanzaDAOPostgresTest {
    private StanzaDAO stanzaDao;

    @BeforeEach
    void setUp() {
        DatabaseHelper.resetTestDatabase();
        stanzaDao = DAOFactory.getDAOFactory().getStanzaDAO();
    }

    @Test
    void testSaveAndFindById() {
        Stanza stanza = stanzaDao.findById("R01");
        assertNotNull(stanza);
        assertEquals("Horror Asylum", stanza.getTema());
    }
}
\end{lstlisting}

\section{Testing Funzionale (Black-Box e Tracciabilità)}

I \texttt{Controllers} sono stati sottoposti a test Black-Box, verificando il comportamento del sistema in base agli output prodotti a fronte di specifici input, in conformità ai template degli Use Case.

Per garantire la massima \textbf{tracciabilità}, ogni test funzionale è stato mappato direttamente su uno specifico \textit{Use Case Template} (Sezione 2.2). Sono stati collaudati sia i \textit{Flussi Base} che i \textit{Flussi Alternativi} (le eccezioni).

\subsection{Catalogo dei Test}
La Tabella \ref{tab:test_catalog_1} e la Tabella \ref{tab:test_catalog_2} riassumono l'intera suite di test, indicando tipo, classe e identificativo.

\begin{table}[htbp]
    \centering
    \small
    \renewcommand{\arraystretch}{1.15}
    \begin{tabularx}{\textwidth}{|>{\bfseries}l|>{\bfseries}l|>{\raggedright\arraybackslash}X|}
        \hline
        ID & Tipo & Test \\ \hline
        U1 & Unit & {\ttfamily\footnotesize StanzaTest.\newline testTransizioneCorretta} \\ \hline
        U2 & Unit & {\ttfamily\footnotesize StanzaTest.\newline testTransizioneInCorso\_TerminaSessione\_VaInPulizia} \\ \hline
        U3 & Unit & {\ttfamily\footnotesize StanzaTest.\newline testTransizioneDisponibile\_ImpostaManutenzione} \\ \hline
        U4 & Unit & {\ttfamily\footnotesize StanzaTest.\newline testCicloCompletoStati} \\ \hline
        U5 & Unit & {\ttfamily\footnotesize StanzaTest.\newline testEccezioneStatoNonValido (+ 3 varianti)} \\ \hline
        U6 & Unit & {\ttfamily\footnotesize PricingStrategyTest.\newline testTariffaBaseCalcoloCorretto} \\ \hline
        U7 & Unit & {\ttfamily\footnotesize PricingStrategyTest.\newline testTariffaWeekendMaggiorazione} \\ \hline
        U8 & Unit & {\ttfamily\footnotesize PricingStrategyTest.\newline testCambioStrategiaARuntimeDaBaseAWeekend} \\ \hline
        U9 & Unit & {\ttfamily\footnotesize WaitingListObserverTest.\newline testAttachENotifica} \\ \hline
        U10 & Unit & {\ttfamily\footnotesize WaitingListObserverTest.\newline testDetachNonRiceveNotifica} \\ \hline
        U11 & Unit & {\ttfamily\footnotesize WaitingListObserverTest.\newline testNotificaMultipleObservers} \\ \hline
        U12 & Unit & {\ttfamily\footnotesize PrenotazioneBuilderTest.\newline testBuilderCreaPrenotazioneCompleta} \\ \hline
        M1 & Mock & {\ttfamily\footnotesize PrenotazioneControllerMockTest.\newline testPrenotazioneRiuscita\_FlussoBase\_UC1} \\ \hline
        M2 & Mock & {\ttfamily\footnotesize PrenotazioneControllerMockTest.\newline testPrenotazioneNegata\_SlotOccupato\_UC1\_Alt3a} \\ \hline
        M3 & Mock & {\ttfamily\footnotesize PrenotazioneControllerMockTest.\newline testPrenotazioneNegata\_CapienzaSuperata\_UC1\_Alt4a} \\ \hline
        M4 & Mock & {\ttfamily\footnotesize GestioneStatoControllerMockTest.\newline testAvviaSessione\_DaDisponibile\_UC3\_FlussoBase} \\ \hline
        M5 & Mock & {\ttfamily\footnotesize GestioneStatoControllerMockTest.\newline testTransizioneNonValida\_UC3\_Alt4a} \\ \hline
        M6 & Mock & {\ttfamily\footnotesize ListaAttesaControllerMockTest.\newline testIscrizioneRiuscita\_UC2\_FlussoBase} \\ \hline
        M7 & Mock & {\ttfamily\footnotesize ListaAttesaControllerMockTest.\newline testIscrizioneDuplicata\_UC2\_Alt5a} \\ \hline
        M8 & Mock & {\ttfamily\footnotesize TariffeControllerMockTest.\newline testCambioTariffaWeekend\_UC4\_FlussoBase} \\ \hline
    \end{tabularx}
    \caption{Catalogo dei test (Parte 1: Unit e Mock Test)}
    \label{tab:test_catalog_1}
\end{table}

\begin{table}[htbp]
    \centering
    \small
    \renewcommand{\arraystretch}{1.15}
    \begin{tabularx}{\textwidth}{|>{\bfseries}l|>{\bfseries}l|>{\raggedright\arraybackslash}X|}
        \hline
        ID & Tipo & Test \\ \hline
        I1 & Integration & {\ttfamily\footnotesize StanzaDAOPostgresTest.\newline testSaveAndFindById} \\ \hline
        I2 & Integration & {\ttfamily\footnotesize PrenotazioneDAOPostgresTest.\newline testIsSlotOccupato} \\ \hline
        F1 & Functional & {\ttfamily\footnotesize PrenotazioneControllerTest.\newline testPrenotazioneNegataPerSlotOccupato\_UC1\_Alt3a} \\ \hline
        F2 & Functional & {\ttfamily\footnotesize ListaAttesaControllerTest.\newline testIscrizioneListaAttesa\_OK} \\ \hline
        F3 & Functional & {\ttfamily\footnotesize ListaAttesaControllerTest.\newline testIscrizioneListaAttesa\_Duplicato} \\ \hline
        F4 & Functional & {\ttfamily\footnotesize GestioneStatoControllerTest.\newline testEseguiTransizioneAvvia} \\ \hline
        F5 & Functional & {\ttfamily\footnotesize GestioneStatoControllerTest.\newline testTransizioneNonValida\_UC3\_Alt4a} \\ \hline
        F6 & Functional & {\ttfamily\footnotesize TariffeControllerTest.\newline testCambioTariffaWeekend\_UC4} \\ \hline
    \end{tabularx}
    \caption{Catalogo dei test (Parte 2: Integration e Functional Test)}
    \label{tab:test_catalog_2}
\end{table}

\subsection{Tracciabilità Use Case \textrightarrow{} Test}
I test funzionali e con mock sono tracciati direttamente sui template del Capitolo 2. La Tabella \ref{tab:traceability} sintetizza la copertura dei flussi base e alternativi.

\begin{table}[H]
    \centering
    \small
    \renewcommand{\arraystretch}{1.2}
    \begin{tabularx}{\textwidth}{|>{\bfseries}l|X|}
        \hline
        Use Case / Flusso & Test ID \\ \hline
        UC1 - Flusso base (prenotazione) & M1 \\ \hline
        UC1 - Alternativa 3a (slot occupato) & M2, F1 \\ \hline
        UC1 - Alternativa 4a (capienza) & M3 \\ \hline
        UC2 - Flusso base (iscrizione) & M6, F2 \\ \hline
        UC2 - Alternativa 5a (duplicato) & M7, F3 \\ \hline
        UC3 - Flusso base (avvio sessione) & M4, F4 \\ \hline
        UC3 - Alternativa 4a (transizione non valida) & M5, F5 \\ \hline
        UC4 - Flusso base (cambio tariffa) & M8, F6 \\ \hline
    \end{tabularx}
    \caption{Matrice di tracciabilità Use Case \textrightarrow{} Test}
    \label{tab:traceability}
\end{table}
